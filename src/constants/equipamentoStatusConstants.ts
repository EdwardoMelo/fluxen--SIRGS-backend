/** Tempo padrão (segundos) sem logs para considerar equipamento offline. */
export const DEFAULT_TIMEOUT_ONLINE_SEGUNDOS = 10;

/** Margem extra (segundos) para latência de rede/processamento na verificação de status. */
export const STATUS_LATENCY_BUFFER_SEGUNDOS = 10;
